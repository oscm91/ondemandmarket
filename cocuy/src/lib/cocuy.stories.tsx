import type { Meta } from '@storybook/react';
import { Cocuy } from './cocuy';

const Story: Meta<typeof Cocuy> = {
  component: Cocuy,
  title: 'Cocuy',
};
export default Story;

export const Primary = {
  args: {},
};
