import type { Meta } from '@storybook/react';
import { EarlyAccess } from './early-access';

const Story: Meta<typeof EarlyAccess> = {
  component: EarlyAccess,
  title: 'Pages/EarlyAccess',
};
export default Story;

export const Primary = {
  args: {},
};
