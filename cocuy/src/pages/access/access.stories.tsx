import type { Meta } from '@storybook/react';
import { Access } from './access';

const Story: Meta<typeof Access> = {
  component: Access,
  title: 'Pages/Access',
};
export default Story;

export const Primary = {
  args: {},
};
